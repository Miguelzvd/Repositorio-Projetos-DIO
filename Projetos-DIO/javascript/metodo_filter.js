// filtra o array fazendo comparações e gerando um novo array com os elementos filtrados.

const frutas = ['maçã fuji', 'mação', 'laranja', 'abacaxi'];

console.log(frutas.filter((fruta) => fruta.includes('maçã')));